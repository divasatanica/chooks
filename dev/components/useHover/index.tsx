import React from 'react';
import Hover from './Hover';

export default function HoverIndex() {
  return (
    <div>
      <h2>Move the mouse onto the text (Also import useMount and useUnmount)</h2>
      <Hover />
    </div>
  )
}